Operating System: Windows 10 or higher
Compiler: g++ / MinGW / MSVC / Code::Blocks / Dev C++ / Visual Studio

C++ Standard: C++11 or higher

Required Headers:
- iostream
- fstream
- sstream
- string
- windows.h   # for Sleep() function (Windows-only)

Optional:
- Text Editor or IDE (Code::Blocks / Visual Studio / VS Code)

Note:
- Tested on Windows using Code::Blocks (g++) and Dev C++
- If using Linux or macOS, replace Sleep() with portable delay alternatives like `std::this_thread::sleep_for`
